/* Undefined */

let variable;

// Check if variable is undefined
if (variable === undefined) {
  //read variable 
console.log(variable );
} 
// Check if variable is not  undefined
else {
  //read variable 
console.log(variable);
}

// Assign a value to the variable
variable = 'Hello';

// Check if variable is undefined again
if (variable === undefined) {
  //read variable 
console.log(variable);
} 
// Check if variable is not  undefined
else {
  //read variable 
console.log(variable);
}